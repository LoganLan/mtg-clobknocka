// Create a div element
const div = document.createElement('div');

// Add the 'bg-white' class to the div
div.classList.add('bg-white');

// Optionally, set the background color directly with JavaScript (if you want inline style)
div.style.backgroundColor = 'white';

// Append the div to the body or another element
document.body.appendChild(div);
